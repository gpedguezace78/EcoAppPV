<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pv.android.app.eco.ecoapppv">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".VentasActivity"
            android:label="@string/title_activity_ventas">
            <intent-filter>
                <action android:name="android.intent.action.VENTASACTIVITY" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ComprasActivity"
            android:label="@string/title_activity_compras">
            <intent-filter>
                <action android:name="android.intent.action.COMPRASACTIVITY" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AlmacenActivity"
            android:label="@string/title_activity_almacen">
            <intent-filter>
                <action android:name="android.intent.action.COMPRASACTIVITY" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity android:name=".PruebaUpdActivity"></activity>
    </application>

</manifest>